[pytest]
testpaths = tests
pythonpath = .
addopts =
    -q
    --strict-markers
    --cov=core
    --cov=routers
    --cov-report=term-missing
    --cov-report=html:htmlcov
markers =
    integration: needs a live database or Supabase; excluded from the default run
